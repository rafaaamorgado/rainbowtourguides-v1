# Rainbow Tour Guides Demo Mode
NEXT_PUBLIC_SITE_URL=http://localhost:5000
NEXT_PUBLIC_ENV=demo
NEXT_PUBLIC_DEMO_MODE=1

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Security Configuration
# CORS: In development/preview (NODE_ENV != production), all origins are allowed
# In production, only domains listed in ALLOWED_ORIGINS are allowed
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:5173,https://yourdomain.com
NODE_ENV=development
